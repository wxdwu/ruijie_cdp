# ────────────────────────────────────────────────────────────────
# 开发模式（APP_ENV=development）配置
# 本地进程运行：APP_ENV=development uvicorn app.main:app --reload --port 8000
# 数据库连「远程云 MySQL」。
# 注意：本文件含真实密钥，已被 .gitignore 忽略，请勿提交。
# ────────────────────────────────────────────────────────────────

# DeepSeek API Configuration (LLM 对话用)
LLM_API_KEY=sk-707e060296124b14a2640b68e56f1efa
LLM_BASE_URL=https://api.deepseek.com

# Embedding API 独立配置（DeepSeek 不支持 /embeddings，需单独配置）
# EMBEDDING_API_KEY=sk-xxx
# EMBEDDING_BASE_URL=https://api.openai.com/v1

# Database Configuration —— 开发模式：远程云 MySQL
DB_HOST=192.168.159.22
DB_PORT=33307
DB_USER=root
DB_PASSWORD=t2yccidfsbseang1
DB_NAME=app_cdp

# 测试模式：限制去重时处理的公司数量（0表示不限制，建议先设置为100-500测试）
DEDUP_TEST_MODE_MAX_COMPANIES=100
